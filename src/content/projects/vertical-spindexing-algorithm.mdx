---
title: 'Vertical Spindexing Algorithm'
category: robotics
summary: 'TODO — Kevin: one sentence on the vertical spindexing algorithm — what it sorts or indexes, and on which robot/season.'
weight: 20
---

{/* Kevin: new entry, requested alongside Swerve Drive. Nothing here is
    invented — no repo link, period, stack, team, or credential — because none
    was given. Fill in what's real:
      - which repo this lives in, if it's public
      - period / season (Into the Deep and Decode both had game pieces that
        suit a spindexer — which season was this for?)
      - stack
      - whether this was a solo build or a team effort (add `collaborative`
        if so, per the pattern in the other robotics entries)
      - the indexing/control logic itself, and what made "vertical" the right
        call over a horizontal spindexer
    Delete this comment when you've written the section. */}

## What it does

TODO — Kevin: describe the vertical spindexing algorithm here.
