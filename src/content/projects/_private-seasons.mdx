---
title: 'Placeholder — private robotics repos'
category: robotics
summary: 'Holding file for the four robotics repositories that were private at build time.'
draft: true
---

{/* Kevin: you named four more robotics repos and said you would make them public:

      - 8393 Decode v1.5
      - 8393 Decode v2
      - 8393 Reefscape
      - 8393 Rebuilt Comp Bot

    None of them were public when I built this, so I did not invent repository
    URLs, periods, or credentials for them. This file is `draft: true`, which
    means it does not appear anywhere on the site.

    When the repos are public, copy `ftc-8393-decode.mdx` once per project,
    fill in the real URL and your role, and delete this file. Nothing else
    needs changing — the category pages pick them up automatically.

    If any of them stay private, that is fine and expected for competition
    code: write the entry with no `repo` link at all. The project pages are
    built to look complete without one. */}
